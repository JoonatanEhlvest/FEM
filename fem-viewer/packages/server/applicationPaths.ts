import path from "path";

export const PROJECT_ROOT = path.resolve("./");

export const CLIENT_PATH = path.join(PROJECT_ROOT, "../client");

export const UPLOAD_DIR = path.join(PROJECT_ROOT, "uploads");
